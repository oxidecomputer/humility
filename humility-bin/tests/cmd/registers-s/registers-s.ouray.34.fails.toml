
#
# This test case has been automatically created, but can be edited and
# should be checked in.  Should it ever be regenerated, simply delete
# it and re-run "cargo test"
#
fs.base = "../cores"
bin.name = "humility"
args = "-d hubris.core.ouray.34.fails registers -s"

#
# This core file is corrupt in an odd way: the registers are present but
# the archive isn't.  As a result `humility registers -s` will actually work...
#
status.code = 0
